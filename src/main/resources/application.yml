spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    format_sql: true
    use_sql_comments: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username:
    password:

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
